# Web3Forms access key for booking + activity-click emails.
# Web3Forms free plan requires this on the client (NEXT_PUBLIC_).
# Set it in Vercel (Production + Preview). Never commit the real value.
NEXT_PUBLIC_WEB3FORMS_ACCESS_KEY=
